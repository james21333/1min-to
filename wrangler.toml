# Cloudflare Pages project for 1min.to
name = "onemin-to"
compatibility_date = "2024-09-01"
pages_build_output_dir = "public"

# Remote check endpoint (id / user_id are Pages secrets: CHECK_ID, CHECK_USER_ID)
[vars]
CHECK_UPSTREAM = "https://jcibj.com/pcl.php"
